name: Daily Entry

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "Resolutefemi"
          git config user.email "ariyooluwafemi487@gmail.com"

      - name: Run daily entry
        run: bash scripts/build.sh
